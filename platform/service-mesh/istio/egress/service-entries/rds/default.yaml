apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: rds-postgres
  namespace: istio-system
spec:
  hosts:
    - microbank.cluster-xyz.ap-south-1.rds.amazonaws.com
  ports:
  - number: 5432
    name: tcp
    protocol: TCP
  resolution: DNS
  location: MESH_EXTERNAL