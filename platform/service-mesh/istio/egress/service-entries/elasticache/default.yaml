apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: elasticache
  namespace: istio-system
spec:
  hosts:
    - microbank-redis.xxxxxx.cache.amazonaws.com
  ports:
  - number: 6379
    name: redis
    protocol: TCP
  resolution: DNS
  location: MESH_EXTERNAL