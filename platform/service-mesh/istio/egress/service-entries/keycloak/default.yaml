apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: keycloak
  namespace: istio-system
spec:
  hosts:
    - keycloak.internal.microbank.local
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  resolution: DNS
  location: MESH_EXTERNAL