apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-transaction-service
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: apigateway-ingress
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              tier: workloads
          podSelector:
            matchLabels:
              network-role: backend
              app.kubernetes.io/name: transaction-service
      ports:
        - protocol: TCP
          port: 8083