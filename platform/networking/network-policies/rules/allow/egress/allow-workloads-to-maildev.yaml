apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-workloads-to-maildev
  namespace: microbank-dev
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  podSelector:
    matchLabels:
      network-role: backend

  policyTypes:
  - Egress

  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          tier: platform
      podSelector:
        matchLabels:
          network-role: platform
          app.kubernetes.io/name: maildev
    ports:
    - protocol: TCP
      port: 1025