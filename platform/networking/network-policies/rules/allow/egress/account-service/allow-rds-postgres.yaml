apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-rds-postgres
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: account-service
  policyTypes:
    - Egress
  egress: []