apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-elasticache-redis
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: auth-service
  policyTypes:
    - Egress
  egress: []